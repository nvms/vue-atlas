import VaLayoutManager from './VaLayoutManager.vue'
import VaLayoutManagerConfig from './VaLayoutManagerConfig.vue'

export default Vue => {
  Vue.component(VaLayoutManager.name, VaLayoutManager)
  Vue.component(VaLayoutManagerConfig.name, VaLayoutManagerConfig)
}
