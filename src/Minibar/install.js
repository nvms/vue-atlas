import VaMinibar from './VaMinibar.vue'
import VaMinibarItem from './VaMinibarItem.vue'

export default Vue => {
  Vue.component(VaMinibar.name, VaMinibar)
  Vue.component(VaMinibarItem.name, VaMinibarItem)
}
